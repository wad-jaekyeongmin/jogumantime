# 업무 스케줄/우선순위 관리봇 - 진행상황

## 프로젝트 요약
- PM 경민님용 슬랙 봇 (DM으로 알림 수신, 일일 브리핑, 주간 리포트)
- 기술: Python (Slack Bolt + FastAPI + APScheduler)
- 위치: `work-bot/`

## 완료된 것

### 코드 구현 (100% 완료)
- 프로젝트 스캐폴드 + 의존성 설치 완료 (`pip install -e ".[dev]"`)
- DB: SQLite 스키마, Notification/CalendarEvent 모델, CRUD
- Core: 알림 파이프라인, 우선순위 스코어링(규칙+AI), 브리핑, 주간리포트, 스케줄러
- Slack Bot: 이벤트 리스너, 슬래시 명령어 4종, Block Kit UI, DM 발송, 인터랙티브 액션
- 연동: Google Calendar 읽기/쓰기, Claude AI 분석
- API: FastAPI 헬스체크, OAuth 콜백
- main.py: Slack Bot + FastAPI + APScheduler 단일 프로세스 실행

### 외부 툴 알림 일원화 (100% 완료)
- **Jira**: 내 활성 이슈(DONE 제외)의 새 댓글 + 멘션 → 3분 폴링
- **Confluence**: 내가 기여한 페이지의 새 댓글 + 멘션 → 3분 폴링 (페이지 제외 기능 있음)
- **Figma**: 내 페이지(이름에 "경민" 포함)의 새 댓글 + 멘션 → 3분 폴링
- 우선순위: 댓글 → MEDIUM (배치), 멘션 → HIGH (즉시 DM)
- 모든 알림이 jogumantime DM 하나로 통합됨

### Slack 앱 설정 (api.slack.com)
- [x] 앱 생성 (이름: `jogumantime`)
- [x] Socket Mode 켜기 + App-Level Token 생성
- [x] Bot Token Scopes 7개 추가
- [x] Event Subscriptions 2개 (message.channels, message.im)
- [x] Slash Commands 4개 (/briefing, /schedule, /priority, /report)
- [x] 워크스페이스 설치 승인 완료
- [x] 봇 실행 확인 (/briefing 정상 응답)

### 테스트 결과
- 우선순위 스코어링 정상 (긴급→URGENT, FYI→LOW, DM+리뷰→MEDIUM)
- 알림 파이프라인 정상 (생성→스코어링→DB저장→즉시/배치 분류)
- Block Kit 메시지 6종 정상 생성
- Jira API 연결 확인 (프로젝트 53개, 활성 이슈 13건)
- Confluence API 연결 확인 (내 페이지 댓글 폴링 정상)
- Figma API 연결 확인 (4월 TASK 파일 댓글 12건 폴링 정상)

## 환경 변수 (.env)
```
SLACK_BOT_TOKEN=xoxb-...
SLACK_APP_TOKEN=xapp-...
SLACK_USER_ID=U07UF7196BC
ATLASSIAN_EMAIL=kyeongminjae@catchtable.co.kr
ATLASSIAN_API_TOKEN=ATATT3x...
ATLASSIAN_DOMAIN=https://catchtable.atlassian.net
FIGMA_API_TOKEN=figd_7-EE...
```

## 남은 할 일

### 선택 연동 (나중에 추가 가능)
- Google Calendar: OAuth credentials.json 필요
- Claude AI: Anthropic API Key 필요 (없으면 규칙 기반 스코어링만)

### 운영
- 기존 슬랙 Jira/Confluence/Figma 알림 채널 연동 끊기
- Figma 파일 추가 시 `config.yaml`의 `figma_poll.file_keys`에 추가
- Confluence 페이지 제외 시 `config.yaml`의 `confluence_poll.exclude_page_ids`에 추가

## 실행 방법
```bash
cd ~/admin-night-demo/work-bot
python3 -m app.main
```

## 파일 구조 요약
```
work-bot/
├── pyproject.toml, .env, config.yaml
├── app/
│   ├── main.py                    # 진입점
│   ├── slack_bot/ (bot, commands, views, dm)
│   ├── core/ (notification, priority, briefing, weekly_report, scheduler)
│   ├── integrations/ (google_calendar, claude_ai, jira, confluence, figma)
│   ├── db/ (database, models, queries)
│   └── api/ (routes)
└── tests/
```
